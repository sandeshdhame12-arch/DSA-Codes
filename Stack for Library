#include <iostream>
#include <string>
using namespace std;

#define MAX 5

class Stack
{
private:
    string books[MAX];
    int top;

public:
    Stack()
    {
        top = -1;
    }

    // Return Book (Push)
    void push(string book)
    {
        if (top == MAX - 1)
        {
            cout << "Stack Overflow! No space for more books.\n";
            return;
        }
        top++;
        books[top] = book;
        cout << book << " returned successfully.\n";
    }

    // Arrange Book (Pop)
    void pop()
    {
        if (top == -1)
        {
            cout << "Stack Underflow! No books to arrange.\n";
            return;
        }
        cout << books[top] << " arranged on the shelf.\n";
        top--;
    }

    // Top Book (Peek)
    void peek()
    {
        if (top == -1)
        {
            cout << "No books in the stack.\n";
            return;
        }
        cout << "Top Book: " << books[top] << endl;
    }

    // Display Stack
    void display()
    {
        if (top == -1)
        {
            cout << "Stack is empty.\n";
            return;
        }

        cout << "\nBooks in Stack (Top to Bottom):\n";
        for (int i = top; i >= 0; i--)
        {
            cout << books[i] << endl;
        }
    }
};

int main()
{
    Stack s;
    int choice;
    string book;

    do
    {
        cout << "\n===== Library Stack Menu =====\n";
        cout << "1. Return Book (Push)\n";
        cout << "2. Arrange Book (Pop)\n";
        cout << "3. Top Book (Peek)\n";
        cout << "4. Display Stack\n";
        cout << "5. Exit\n";
        cout << "Enter your choice: ";
        cin >> choice;

        switch (choice)
        {
        case 1:
            cin.ignore();
            cout << "Enter Book Name: ";
            getline(cin, book);
            s.push(book);
            break;

        case 2:
            s.pop();
            break;

        case 3:
            s.peek();
            break;

        case 4:
            s.display();
            break;

        case 5:
            cout << "Exiting Program...\n";
            break;

        default:
            cout << "Invalid Choice!\n";
        }

    } while (choice != 5);

    return 0;
}

OUTPUT:

===== Library Stack Menu =====
1. Return Book (Push)
2. Arrange Book (Pop)
3. Top Book (Peek)
4. Display Stack
5. Exit
Enter your choice: 1
Enter Book Name: DSA Book
DSA Book  returned successfully.

===== Library Stack Menu =====
1. Return Book (Push)
2. Arrange Book (Pop)
3. Top Book (Peek)
4. Display Stack
5. Exit
Enter your choice: 1
Enter Book Name: Maths Book
Maths Book returned successfully.

===== Library Stack Menu =====
1. Return Book (Push)
2. Arrange Book (Pop)
3. Top Book (Peek)
4. Display Stack
5. Exit
Enter your choice: 1
Enter Book Name: PBL BOOK
PBL BOOK returned successfully.

===== Library Stack Menu =====
1. Return Book (Push)
2. Arrange Book (Pop)
3. Top Book (Peek)
4. Display Stack
5. Exit
Enter your choice: 1
Enter Book Name: Atomic Habits
Atomic Habits returned successfully.

===== Library Stack Menu =====
1. Return Book (Push)
2. Arrange Book (Pop)
3. Top Book (Peek)
4. Display Stack
5. Exit
Enter your choice: 2
Atomic Habits arranged on the shelf.

===== Library Stack Menu =====
1. Return Book (Push)
2. Arrange Book (Pop)
3. Top Book (Peek)
4. Display Stack
5. Exit
Enter your choice: 3
Top Book: PBL BOOK

===== Library Stack Menu =====
1. Return Book (Push)
2. Arrange Book (Pop)
3. Top Book (Peek)
4. Display Stack
5. Exit
Enter your choice: 4

Books in Stack (Top to Bottom):
PBL BOOK
Maths Book
DSA Book
